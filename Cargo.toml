[workspace]
members = [
    ".",
    "kiss-plugin-sdk",
    "kiss-url-shortener",
]

[package]
name = "kiss-server"
version = "1.5.0"
edition = "2021"

[dependencies]
kiss-plugin-sdk = { path = "./kiss-plugin-sdk" }
kiss-url-shortener = { path = "./kiss-url-shortener" }
log = { version = "0.4.20", features = ["std"] }
