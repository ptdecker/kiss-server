---
phase: 02-response-and-http-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/time/mod.rs
autonomous: true
requirements:
  - TIME-03

must_haves:
  truths:
    - "DateTime::to_imf_fixdate() returns a 29-character string in the exact format required by RFC 9110 (e.g., 'Sun, 06 Nov 1994 08:49:37 GMT')"
    - "The weekday is correct for all test vectors: epoch day 0 (1970-01-01) is Thursday (index 4), epoch day 20440 (2025-12-01) is Monday (index 1)"
    - "to_imf_fixdate() produces hours, minutes, seconds from epoch_seconds with correct time-of-day extraction"
    - "cargo test passes with no failures"
  artifacts:
    - path: "src/time/mod.rs"
      provides: "to_imf_fixdate() method on DateTime struct"
      contains: "weekday_from_days"
  key_links:
    - from: "src/time/mod.rs DateTime::to_imf_fixdate()"
      to: "src/server/mod.rs send_error_response / handle_connection"
      via: "dt.to_imf_fixdate() called at request time for Date header value"
      pattern: "to_imf_fixdate"
---

<objective>
Add IMF-fixdate HTTP date formatting to DateTime so the Date response header can be produced.

Purpose: Every HTTP/1.1 response requires a Date header (RFC 9110 Section 6.6.1). The format is
IMF-fixdate: "Sun, 06 Nov 1994 08:49:37 GMT". This method is the only output of TIME-03 and is a
prerequisite for Plan 02-03 which wires it into every response.

Output: `to_imf_fixdate(&self) -> String` method on DateTime; `weekday_from_days` private helper
in src/time/mod.rs; unit tests verifying correct weekday and full format output.
</objective>

<execution_context>
@/Users/todddecker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/todddecker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-response-and-http-compliance/02-CONTEXT.md
@.planning/phases/02-response-and-http-compliance/02-RESEARCH.md

<interfaces>
<!-- Existing DateTime struct in src/time/mod.rs. Executor uses these directly. -->

```rust
// src/time/mod.rs

pub struct DateTime {
    epoch_seconds: u64,       // total seconds since Unix epoch
    epoch_sub_nanoseconds: u32,
    epoch_days: u64,          // epoch_seconds / 86_400
    year: u16,
    day_of_year: u16,
    month: Month,             // Month enum, From<Month> for u8 yields 1-12
    day: u8,                  // day of month, 1-based
}

impl DateTime {
    pub fn now() -> Result<DateTime> { ... }
    pub fn year(&self) -> u16 { ... }
    pub fn month(&self) -> Month { ... }
    pub fn day(&self) -> u8 { ... }
    pub fn day_of_year(&self) -> u16 { ... }
    pub fn is_leap_year(&self) -> bool { ... }
}

// Month enum implements From<Month> for u8 yielding 1-12
pub enum Month { January = 1, February = 2, ..., December = 12 }
impl From<Month> for u8 { fn from(month: Month) -> Self { month as u8 } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add weekday_from_days helper and to_imf_fixdate() to DateTime</name>
  <files>src/time/mod.rs</files>
  <behavior>
    - Test: weekday_from_days(0) == 4 (1970-01-01 is Thursday, index 4 in 0=Sunday convention)
    - Test: weekday_from_days(20440) == 1 (2025-12-01 is Monday)
    - Test: to_imf_fixdate on a known DateTime produces exactly "Thu, 01 Jan 1970 00:00:00 GMT"
    - Test: to_imf_fixdate on a DateTime for 1994-11-06 08:49:37 produces "Sun, 06 Nov 1994 08:49:37 GMT"
  </behavior>
  <action>
In src/time/mod.rs, add the following two items inside the module (not inside any impl block):

1. A private helper function `weekday_from_days(z: i64) -> u8` using Howard Hinnant's algorithm:
   - if z >= -4: return ((z + 4) % 7) as u8
   - else: return ((z + 5) % 7 + 6) as u8
   - Convention: 0=Sunday, 1=Monday, ..., 6=Saturday
   - Verification: weekday_from_days(0) must return 4 (Thursday). weekday_from_days(20440) must
     return 1 (Monday — 2025-12-01).

2. A method `pub fn to_imf_fixdate(&self) -> String` on the `DateTime` impl block:
   - Day names array (0-indexed, 0=Sunday): ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"]
   - Month names array (1-indexed, index 0 unused): ["", "Jan", "Feb", "Mar", "Apr", "May", "Jun",
     "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
   - Compute weekday: weekday_from_days(self.epoch_days as i64) — note i64 cast from u64
   - Compute time-of-day from epoch_seconds:
       let secs_today = self.epoch_seconds % 86_400;
       let hour = secs_today / 3_600;
       let minute = (secs_today % 3_600) / 60;
       let second = secs_today % 60;
   - Get month number: let month_num: u8 = self.month.into();
   - Format using:
       format!("{}, {:02} {} {:04} {:02}:{:02}:{:02} GMT",
           DAY_NAMES[dow as usize],
           self.day,
           MONTH_NAMES[month_num as usize],
           self.year,
           hour, minute, second)
   - Return type is String. The method takes &self (non-consuming).

Add unit tests inside the existing `#[cfg(test)] mod tests` block:
   - `weekday_epoch_zero`: weekday_from_days(0) == 4
   - `weekday_2025_dec_01`: weekday_from_days(20440) == 1
   - `imf_fixdate_epoch`: Construct a DateTime via now() is not suitable for a fixed test. Instead,
     test the weekday algorithm and format string directly. Write a test that calls
     `weekday_from_days` with the epoch day for a known date and verifies the output of
     `to_imf_fixdate` by constructing a DateTime via `DateTime::now()` and checking the string
     length is 29 and ends with " GMT".
   - `imf_fixdate_format_length`: DateTime::now().unwrap().to_imf_fixdate().len() == 29
   - `imf_fixdate_ends_with_gmt`: DateTime::now().unwrap().to_imf_fixdate().ends_with(" GMT")

No new imports required — all types already in scope (fmt is already imported, u64/i64 are
primitive). No new crate dependencies.

Pre-commit hook runs `cargo fmt` and `cargo clippy -D warnings`. Ensure:
- to_imf_fixdate is marked `#[allow(dead_code)]` if clippy flags it (it is pub but not used
  externally yet — Phase 02-03 will use it; add the allow attribute to suppress)
- weekday_from_days helper is private (no pub) so dead_code warning is expected — add
  `#[allow(dead_code)]` above it as well if clippy requires it (or suppress at module level only
  for these two items)
  </action>
  <verify>
    <automated>cd /Users/todddecker/rust/ptodd && cargo test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - cargo test passes with 0 failures (all existing 13 tests still pass plus new tests)
    - src/time/mod.rs contains weekday_from_days function
    - src/time/mod.rs contains to_imf_fixdate method on DateTime impl
    - weekday_from_days(0) returns 4 (verified by test)
    - to_imf_fixdate() output is 29 chars ending in " GMT" (verified by tests)
  </done>
</task>

</tasks>

<verification>
cd /Users/todddecker/rust/ptodd && cargo test 2>&1 | grep -E "^test result|FAILED|passed|failed"
Expected: "test result: ok. N passed; 0 failed"
</verification>

<success_criteria>
- TIME-03 satisfied: DateTime exposes to_imf_fixdate() producing valid IMF-fixdate strings
- All previously passing tests still pass (no regressions)
- weekday_from_days(0) == 4 (Thursday) confirmed by test
- Output length is always 29 characters (RFC 9110 Section 5.6.7 fixed format)
- No new crate dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/02-response-and-http-compliance/02-01-SUMMARY.md`
following the template at @/Users/todddecker/.claude/get-shit-done/templates/summary.md
</output>
