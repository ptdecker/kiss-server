---
phase: 10.1-rename-cargo-package-and-directory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - scripts/install-kiss-server.sh
  - Justfile
  - README.md
  - src/handlers/mod.rs
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08

must_haves:
  truths:
    - "cargo build --release produces target/release/kiss-server (not target/release/ptodd)"
    - "All existing tests pass after rename (cargo test exits 0)"
    - "install-kiss-server.sh references /opt/kiss-server and target/release/kiss-server consistently"
    - "No stale ptodd binary/package/clone-dir references remain (domain ptodd.org references are unchanged)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Package name kiss-server"
      contains: 'name = "kiss-server"'
    - path: "scripts/install-kiss-server.sh"
      provides: "Updated clone dir and binary source"
      contains: 'CLONE_DIR="/opt/kiss-server"'
    - path: "Justfile"
      provides: "Updated cargo doc path"
      contains: "target/doc/kiss-server/index.html"
    - path: "README.md"
      provides: "Corrected git clone URL"
      contains: "ptdecker/kiss-server.git"
  key_links:
    - from: "Cargo.toml"
      to: "target/release/kiss-server"
      via: "cargo build derives binary name from package name"
      pattern: 'name = "kiss-server"'
    - from: "scripts/install-kiss-server.sh"
      to: "Cargo.toml"
      via: "BINARY_SRC must match the binary name Cargo produces"
      pattern: "target/release/kiss-server"
---

<objective>
Rename the Cargo package from `ptodd` to `kiss-server` and update all references to the binary name and clone directory across the codebase.

Purpose: The compiled binary name should match the deployed service name (`kiss-server`) without a post-build rename step. The EC2 clone directory should also use the canonical name.
Output: All files updated, `cargo build --release` produces `target/release/kiss-server`, all tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-rename-cargo-package-and-directory/10.1-CONTEXT.md
@.planning/phases/10.1-rename-cargo-package-and-directory/10.1-RESEARCH.md

@Cargo.toml
@scripts/install-kiss-server.sh
@Justfile
@README.md
@src/handlers/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Cargo package and update install script</name>
  <files>Cargo.toml, scripts/install-kiss-server.sh</files>
  <read_first>Cargo.toml, scripts/install-kiss-server.sh</read_first>
  <action>
**Cargo.toml (per D-01):**
Change line 2 from `name = "ptodd"` to `name = "kiss-server"`. Leave all other fields unchanged.

After the edit, `Cargo.toml` should read:
```toml
[package]
name = "kiss-server"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
log = { version = "0.4.20", features = ["std"] }
```

Do NOT manually edit `Cargo.lock` — `cargo build` regenerates it automatically.
Do NOT add a `[[bin]]` section — the `[package] name` field is sufficient for a single binary crate.

**scripts/install-kiss-server.sh (per D-03, D-04, D-05, D-06):**

Line 16 (comment): Change `Clone or update repo at /opt/ptodd` to `Clone or update repo at /opt/kiss-server`

Line 23: Change `CLONE_DIR="/opt/ptodd"` to `CLONE_DIR="/opt/kiss-server"` (per D-04)

Line 26: Change `BINARY_SRC="$CLONE_DIR/target/release/ptodd"` to `BINARY_SRC="$CLONE_DIR/target/release/kiss-server"` (per D-05)

Line 104: Change the Step 6 header from:
`# ─── Step 6: Install binary (rename ptodd → kiss-server) ─────────────────────`
to:
`# ─── Step 6: Install binary ──────────────────────────────────────────────────`
(per D-06 — no rename step needed since Cargo now produces `kiss-server` directly)

Do NOT touch line 30 (`WEBROOT="/var/www/ptodd.org"`) — that is a domain path, not a binary/package reference.
Do NOT touch line 137 (`ExecStart=/usr/local/bin/kiss-server --root /var/www/ptodd.org`) — the `ptodd.org` portion is a domain path.

EC2 directory migration approach (Claude's discretion): Use Option A from research — just update the script variables. On the next run, the script does a fresh clone to `/opt/kiss-server`. The old `/opt/ptodd` directory becomes an orphan on the instance (safe — contains only regenerable source and build artifacts).
  </action>
  <verify>
    <automated>cargo build --release 2>&1 && test -f target/release/kiss-server && echo "PASS: binary exists" || echo "FAIL: binary missing"; cargo test --locked 2>&1 | tail -5; grep 'name = "kiss-server"' Cargo.toml && echo "PASS: Cargo.toml"; grep 'CLONE_DIR="/opt/kiss-server"' scripts/install-kiss-server.sh && echo "PASS: CLONE_DIR"; grep 'target/release/kiss-server' scripts/install-kiss-server.sh && echo "PASS: BINARY_SRC"</automated>
  </verify>
  <acceptance_criteria>
- `Cargo.toml` line 2 contains `name = "kiss-server"`
- `target/release/kiss-server` exists after `cargo build --release`
- `cargo test --locked` exits 0
- `scripts/install-kiss-server.sh` line 23 contains `CLONE_DIR="/opt/kiss-server"`
- `scripts/install-kiss-server.sh` line 26 contains `target/release/kiss-server`
- `scripts/install-kiss-server.sh` Step 6 comment does NOT contain `rename ptodd`
- `scripts/install-kiss-server.sh` line 30 still contains `WEBROOT="/var/www/ptodd.org"` (unchanged)
  </acceptance_criteria>
  <done>Cargo package renamed to kiss-server, binary compiles with correct name, install script variables updated, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update remaining ptodd references in Justfile, README, and src (per D-07, D-08)</name>
  <files>Justfile, README.md, src/handlers/mod.rs</files>
  <read_first>Justfile, README.md, src/handlers/mod.rs</read_first>
  <action>
**Justfile (per D-07, D-08):**
Line 24: Change `@open target/doc/ptodd/index.html` to `@open target/doc/kiss-server/index.html`

**README.md (per D-07, D-08):**
Line 22: Change `git clone git@github.com:ptdecker/ptodd.git` to `git clone git@github.com:ptdecker/kiss-server.git`

Do NOT change line 1 (`# ptodd.org Backend Framework`) — `ptodd.org` is the domain name, which is unchanged.
Do NOT change line 3 (`...supporting ptodd.org.`) — same reason, domain reference.

**src/handlers/mod.rs (discretionary — rename for consistency):**
Line 246: Change `format!("ptodd_test_{}", name)` to `format!("kiss_server_test_{}", name)`
Line 355: Change `"ptodd_test_nonexistent_zzz_should_not_exist"` to `"kiss_server_test_nonexistent_zzz_should_not_exist"`

Both occurrences must be updated together. These are temp directory name prefixes used in tests — functionally irrelevant but updated for consistency with the new package name.

**src/main.rs — NO CHANGE needed:**
Line 1 (`//! Provides the backend implementation for the ptodd.org website.`) references the domain `ptodd.org`, not the binary/package name. Leave it as-is.

**Final audit after edits:** Run grep for any remaining `ptodd` references that are NOT domain references (`ptodd.org`). Expected: zero matches outside of `ptodd.org` domain strings.
  </action>
  <verify>
    <automated>grep 'target/doc/kiss-server/index.html' Justfile && echo "PASS: Justfile"; grep 'ptdecker/kiss-server.git' README.md && echo "PASS: README"; grep 'kiss_server_test_' src/handlers/mod.rs | head -2 && echo "PASS: test strings"; cargo test --locked 2>&1 | tail -3; cargo fmt --check 2>&1; cargo clippy --locked --all-targets -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `Justfile` line 24 contains `target/doc/kiss-server/index.html`
- `README.md` line 22 contains `ptdecker/kiss-server.git`
- `README.md` line 1 still contains `ptodd.org` (domain, unchanged)
- `src/handlers/mod.rs` contains `kiss_server_test_` (two occurrences)
- `src/handlers/mod.rs` does NOT contain `ptodd_test_`
- `src/main.rs` line 1 still contains `ptodd.org` (domain, unchanged)
- `cargo test --locked` exits 0
- `cargo fmt --check` exits 0
- `cargo clippy --locked --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>All non-domain ptodd references updated across Justfile, README.md, and src/handlers/mod.rs. Full lint and test suite passes.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full phase gate:

```bash
# Binary name correct
cargo build --release && test -f target/release/kiss-server

# Full CI checks pass
cargo fmt --check
cargo clippy --locked --all-targets -- -D warnings
cargo test --locked

# No stale ptodd references (excluding domain ptodd.org)
grep -rn 'ptodd' --include='*.toml' --include='*.sh' --include='*.rs' --include='*.md' --include='Justfile' . | grep -v 'ptodd\.org' | grep -v 'target/' | grep -v '.planning/'
# Expected: zero matches
```
</verification>

<success_criteria>
- `cargo build --release` produces `target/release/kiss-server`
- `cargo test --locked` exits 0 with all tests passing
- `cargo fmt --check` exits 0
- `cargo clippy --locked --all-targets -- -D warnings` exits 0
- No `ptodd` references remain in source/scripts/config files outside of `ptodd.org` domain strings
- `scripts/install-kiss-server.sh` uses `/opt/kiss-server` clone dir and `target/release/kiss-server` binary source
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-rename-cargo-package-and-directory/10.1-01-SUMMARY.md`
</output>
