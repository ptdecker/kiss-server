---
phase: 05.1-address-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-static-file-serving/05-03-SUMMARY.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "REQUIREMENTS.md traceability row for PATH-03 shows Phase 5, not Phase 4"
    - "05-03-SUMMARY.md does not say --root is required anywhere it describes FILE-03 behavior"
    - "05-03-SUMMARY.md correctly reflects that --root is optional and defaults to '.'"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate PATH-03 traceability"
      contains: "PATH-03 | Phase 5 | Complete"
    - path: ".planning/phases/05-static-file-serving/05-03-SUMMARY.md"
      provides: "Accurate FILE-03 description"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "PATH-03 row"
      via: "Traceability table"
      pattern: "PATH-03.*Phase 5"
---

<objective>
Fix two planning document inaccuracies identified in the v1.0 milestone audit:
1. REQUIREMENTS.md PATH-03 traceability row still maps to Phase 4 — PATH-03 was actually completed in Phase 5 (StaticFileHandler's canonicalize + starts_with guard). Update the Phase column from "Phase 4" to "Phase 5".
2. 05-03-SUMMARY.md says "--root is required" in the context of FILE-03 — the implementation defaults to "." when --root is absent (confirmed by parse_root_from test `parse_root_from_no_root_flag_defaults_to_current_dir`). Correct the text.

Purpose: Planning documents are the historical record for this project. Inaccurate traceability and incorrect feature descriptions create confusion during future maintenance and milestone reviews.

Output: Both files corrected, grep checks pass, no code changes.
</objective>

<execution_context>
@/Users/todddecker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/todddecker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-address-tech-debt/05.1-CONTEXT.md
@.planning/phases/05.1-address-tech-debt/05.1-RESEARCH.md

<interfaces>
<!-- Exact locations confirmed by direct file inspection. -->

From .planning/REQUIREMENTS.md — Traceability table (line 107):
Current:  `| PATH-03 | Phase 4 | Complete |`
Fixed:    `| PATH-03 | Phase 5 | Complete |`

Change: only the "Phase 4" → "Phase 5" text in that one cell. Status column already reads "Complete" — do not touch it.

From .planning/phases/05-static-file-serving/05-03-SUMMARY.md:
The audit evidence: "SUMMARY says '--root required', implementation defaults to '.' when --root absent"

Search the file for the incorrect claim. The Accomplishments section (line 67) reads:
  `cargo run (no args) exits with Error: "--root <path> is required" and non-zero exit code`

This accurately describes the CURRENT behavior — when no --root is provided, the server exits with that error message. This is NOT the inaccuracy.

The actual inaccuracy is in how FILE-03 is characterized: FILE-03 says "Static file root directory is configurable via CLI argument at startup" — the original plan may have described --root as a required argument when the final implementation makes it optional (defaults to ".").

Search 05-03-SUMMARY.md for any text claiming --root is "required" in the context of describing how --root works (as opposed to the literal error message string). The key line to fix is wherever the SUMMARY describes the --root argument as required rather than optional-with-default.

Look for: text describing --root as mandatory/required that doesn't match the implementation behavior where absence of --root defaults to "." (current directory).

If no such text exists beyond the literal error message string, confirm that and note it. The literal error message `"--root <path> is required"` in the Accomplishments section is a description of what the binary prints — that is accurate. The fix target is any narrative text that mischaracterizes --root as a required argument rather than an optional one with a default.

Correction: if found, update the narrative to say --root is optional and defaults to "." (current directory). If the only reference is the literal error message text, that is a false alarm from the audit — note it and leave the file as-is (no change needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix REQUIREMENTS.md PATH-03 traceability and verify 05-03-SUMMARY.md FILE-03 text</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/05-static-file-serving/05-03-SUMMARY.md</files>
  <action>
Step 1 — Fix REQUIREMENTS.md:
Find the PATH-03 row in the Traceability table (currently line 107). The row reads:
  `| PATH-03 | Phase 4 | Complete |`

Change "Phase 4" to "Phase 5":
  `| PATH-03 | Phase 5 | Complete |`

Only this one cell changes. Do not alter the Status column, surrounding rows, or any other content.

Step 2 — Verify and fix 05-03-SUMMARY.md:
Read the full file to locate any narrative text that describes --root as required (beyond the literal error message string `"--root <path> is required"` which accurately describes what the binary prints).

The implementation facts:
- parse_root_from() returns Ok(PathBuf::from(".")) when no --root flag is present
- The test `parse_root_from_no_root_flag_defaults_to_current_dir` confirms this
- `cargo run` (no args) currently exits with error — BUT that is because parse_root_from returns Err when no --root is given... wait: re-read the implementation.

IMPORTANT: Check which behavior is actually implemented. The Accomplishments line says:
  "`cargo run` (no args) exits with `Error: \"--root <path> is required\"` and non-zero exit code"

This means the CURRENT implementation treats --root as required (exits with error when absent). But the RESEARCH.md says "implementation defaults to '.' when --root absent". Read src/main.rs to resolve this conflict before making any edit to 05-03-SUMMARY.md.

If the implementation exits with error when --root is absent, then the SUMMARY text is accurate and no change is needed to 05-03-SUMMARY.md.
If the implementation defaults to ".", update any text that says --root is required.

Only make changes that are actually incorrect. Do not manufacture corrections.
  </action>
  <verify>
    <automated>grep "PATH-03" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
REQUIREMENTS.md: `grep "PATH-03" .planning/REQUIREMENTS.md` shows `| PATH-03 | Phase 5 | Complete |`.

05-03-SUMMARY.md: either corrected to accurately reflect --root behavior, or confirmed accurate as-is (no change needed) after reading src/main.rs.
  </done>
</task>

</tasks>

<verification>
Grep checks:

```bash
grep "PATH-03" .planning/REQUIREMENTS.md
# Expected: | PATH-03 | Phase 5 | Complete |

grep -i "required" .planning/phases/05-static-file-serving/05-03-SUMMARY.md
# Review output: error message string literal is fine; narrative claiming --root is required is the target
```
</verification>

<success_criteria>
- `grep "PATH-03" .planning/REQUIREMENTS.md` returns a line with "Phase 5" not "Phase 4"
- 05-03-SUMMARY.md accurately describes --root behavior (either corrected or confirmed accurate after checking src/main.rs)
- No code files modified — this plan touches only planning documents
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-address-tech-debt/05.1-02-SUMMARY.md`
</output>
