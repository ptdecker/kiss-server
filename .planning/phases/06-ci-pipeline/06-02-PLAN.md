---
phase: 06-ci-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified: []
autonomous: false
requirements:
  - CI-01
  - CI-02
  - CI-03
  - CI-04
  - CI-06

must_haves:
  truths:
    - "Pushing to main triggers a CI run visible in the GitHub Actions tab"
    - "Opening a PR to main triggers a CI run and posts a status check on the PR"
    - "A PR with a formatting error shows a red CI status check"
    - "A PR with a clippy warning shows a red CI status check"
    - "A PR with a failing test shows a red CI status check"
    - "A second CI run shows cache restoration in the Swatinem/rust-cache step log"
  artifacts: []
  key_links:
    - from: "GitHub Actions tab"
      to: "ci job"
      via: "workflow trigger on push/PR to main"
      pattern: "job named 'ci' appears in checks list"
---

<objective>
Push the CI artifacts to GitHub, observe the first CI run, then verify CI failure modes and cache behavior work correctly.

Purpose: Confirms all 6 CI requirements are satisfied in the actual GitHub environment. Local verification (Plan 01) proves scripts are correct; this plan proves GitHub Actions executes them as intended.
Output: Verified green CI run, observed red CI on bad PRs, cache confirmed on second run.
</objective>

<execution_context>
@/Users/todddecker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/todddecker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ci-pipeline/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push to main and trigger first CI run</name>
  <files></files>
  <action>
If Plan 01 changes are not yet committed and pushed to main, do so now:
```bash
git add rust-toolchain.toml scripts/ci.sh .github/workflows/ci.yml
git status  # confirm rust.yml is removed (should already be via git rm)
git commit -m "ci: add CI workflow, toolchain pin, and local ci.sh script"
git push origin main
```

After push, wait ~60 seconds for the Actions run to complete, then check:
```bash
gh run list --workflow=ci.yml --limit=3
```

The run triggered by this push must show status `completed` and conclusion `success`.
  </action>
  <verify>
    <automated>gh run list --workflow=ci.yml --limit=1 --json status,conclusion,event | grep -q '"success"'</automated>
  </verify>
  <done>First CI run on main is green; visible in GitHub Actions tab under the 'ci' job name</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CI gates and cache in GitHub</name>
  <action>
Human verification of CI failure modes (CI-02, CI-03, CI-04) and cache behavior (CI-06).
All verifications require GitHub Actions to be running after Plan 01 push.
  </action>
  <verify>
    <automated>MISSING — CI gate verification requires GitHub PR interaction (manual only)</automated>
  </verify>
  <done>All 5 verifications approved: green first run, red on fmt/clippy/test errors, cache restored on second run</done>
  <what-built>
    CI workflow (ci.yml) pushed to main. First CI run completed. Three failure mode verifications needed:
    1. fmt gate (CI-02): Does a formatting error fail CI?
    2. clippy gate (CI-03): Does a clippy warning fail CI?
    3. test gate (CI-04): Does a failing test fail CI?
    4. Cache (CI-06): Does the second run show cache restoration?
  </what-built>
  <how-to-verify>
    **Step 1 — Confirm first run succeeded:**
    Visit https://github.com/[your-repo]/actions and verify the most recent 'ci' run is green.

    **Step 2 — Verify fmt gate (CI-02):**
    Create a branch, introduce a formatting error (e.g., remove spaces around `=` in any .rs file), open a PR to main. Verify the CI check shows red. Close the PR without merging.

    **Step 3 — Verify clippy gate (CI-03):**
    Create a branch, add `let _x = 5;` (or `let x = 5;` with no usage) to any function in src/. Open a PR to main. Verify CI shows red. Close the PR without merging.

    **Step 4 — Verify test gate (CI-04):**
    Create a branch, change any `assert_eq!` to use a wrong expected value. Open a PR to main. Verify CI shows red. Close the PR without merging.

    **Step 5 — Verify cache (CI-06):**
    After at least two CI runs exist, open the second (or later) run in the Actions tab. Click on the 'ci' job. Find the "Swatinem/rust-cache" step. Its log must contain "Restored cache" (not "No cache found").

    All 5 checks must pass before approving.
  </how-to-verify>
  <resume-signal>Type "approved" when all 5 verifications pass, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
All 6 CI requirements verified:
- CI-01: CI runs on push to main and on PR targeting main
- CI-02: Formatting error → red CI check
- CI-03: Clippy warning → red CI check
- CI-04: Failing test → red CI check
- CI-05: rust-toolchain.toml exists and CI log shows toolchain 1.93.1 (visible in dtolnay/rust-toolchain step)
- CI-06: Second CI run shows "Restored cache" in Swatinem/rust-cache step
</verification>

<success_criteria>
- First CI run on main is green (all steps pass)
- Three separate PRs demonstrate red CI on fmt error, clippy warning, and test failure
- Second CI run shows cache restoration from Swatinem/rust-cache
- GitHub Actions tab shows job named 'ci' (not 'build') — required for Phase 7 branch protection
</success_criteria>

<output>
After completion, create `.planning/phases/06-ci-pipeline/06-02-SUMMARY.md`
</output>
