---
phase: 12.1-clean-up-in-aisle-9
plan: "01"
subsystem: rust-server
tags: [rust, dead-code, clippy, x-powered-by, tdd]

# Dependency graph
requires:
  - phase: 10.1-rename-cargo-package-and-directory
    provides: kiss-server binary name; CARGO_PKG_VERSION set to 0.1.0
provides:
  - Dead code removed from url/mod.rs, time/mod.rs, server/response.rs, server/mod.rs
  - pct_encode and pct_decode relocated inside #[cfg(test)] block in url/mod.rs
  - X-Powered-By response header injected in normal HTTP responses
  - ENABLE_POWERED_BY const gate for the header
affects: [12.1-02, 12.1-03, 12.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Test-only helpers in #[cfg(test)] block, not module scope with #[allow(dead_code)]"
    - "Compile-time const flag (ENABLE_POWERED_BY) gates optional header injection"
    - "concat! + env!(CARGO_PKG_VERSION) for zero-cost compile-time version string"

key-files:
  created: []
  modified:
    - src/url/mod.rs
    - src/time/mod.rs
    - src/server/response.rs
    - src/server/mod.rs

key-decisions:
  - "Retain #[allow(clippy::wrong_self_convention)] on to_imf_fixdate — clippy's self-convention lint fires on &self methods returning owned String; the to_ prefix is correct semantics here"
  - "ENABLE_POWERED_BY = true at module scope — compile-time flag pattern allows zero-cost opt-out without runtime overhead"
  - "X-Powered-By uses kiss-serve (not kiss-server) per user spec; server binary is kiss-server but header value is kiss-serve"

patterns-established:
  - "Dead code pattern: test helpers belong in #[cfg(test)] block, not at module scope with #[allow(dead_code)]"
  - "Header injection gate: const ENABLE_POWERED_BY: bool = true followed by if ENABLE_POWERED_BY block in handle_connection"

requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-06, D-14, D-32]

# Metrics
duration: 4min
completed: "2026-03-25"
---

# Phase 12.1 Plan 01: Clean Up Dead Code Summary

**Dead code removed and test helpers relocated to #[cfg(test)]; X-Powered-By: kiss-serve/0.1.0 header injected in all normal HTTP responses via compile-time const gate**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-03-25T17:04:08Z
- **Completed:** 2026-03-25T17:07:33Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Removed all spurious `#[allow(dead_code)]` suppressions on genuinely unused code in url/mod.rs, time/mod.rs, and response.rs
- Deleted 7 functions with no callers: `days_in_month`, `year()`, `month()`, `day()`, `day_of_year()`, `is_leap_year()` (DateTime methods), and relocated `pct_encode`/`pct_decode` into `#[cfg(test)]`
- Deleted TODO comment blocks in server/mod.rs (replaced by GitHub issues #24 and #25)
- Added `X-Powered-By: kiss-serve/0.1.0` header to normal HTTP responses; error responses are unaffected
- All 87 tests pass (86 original + 1 new X-Powered-By integration test); clippy clean with `-D warnings`

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove dead code and relocate test-only functions** - `998869c` (refactor)
2. **Task 2: X-Powered-By — TDD RED (failing test)** - `7253741` (test)
3. **Task 2: X-Powered-By — TDD GREEN (implementation)** - `da7886f` (feat)

_Note: TDD task 2 has two commits (test RED → feat GREEN); no refactor phase needed._

## Files Created/Modified
- `src/url/mod.rs` - Removed module-scope `pct_encode`/`pct_decode` with `#[allow(dead_code)]`; moved both inside `#[cfg(test)]` block; moved `from_utf8` import into test block
- `src/time/mod.rs` - Deleted `days_in_month` function; deleted 5 dead DateTime accessor methods; removed `#[allow(dead_code)]` from `weekday_from_days` and `to_imf_fixdate`; retained `#[allow(clippy::wrong_self_convention)]` on `to_imf_fixdate`
- `src/server/response.rs` - Removed `#[allow(dead_code)]` from `impl Response` block
- `src/server/mod.rs` - Deleted TODO comment blocks; added `ENABLE_POWERED_BY` const; added X-Powered-By header injection after Date header in `handle_connection`; added integration test

## Decisions Made
- Retained `#[allow(clippy::wrong_self_convention)]` on `to_imf_fixdate` after clippy complained — `to_` methods taking `&self` trigger this lint but the method semantics are correct (returns owned String); the dead_code allow was removed as planned
- Header value uses `kiss-serve` (not `kiss-server`) per the plan specification

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Re-added clippy::wrong_self_convention allow on to_imf_fixdate**
- **Found during:** Task 1 (remove dead code)
- **Issue:** Plan said remove both `#[allow(dead_code)]` and `#[allow(clippy::wrong_self_convention)]` and re-add the clippy allow only if clippy complains — clippy did complain (1 error on `to_` method with `&self`)
- **Fix:** Re-added `#[allow(clippy::wrong_self_convention)]` only; `#[allow(dead_code)]` was removed as intended
- **Files modified:** src/time/mod.rs
- **Verification:** `cargo clippy -- -D warnings` exits 0
- **Committed in:** 998869c (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — plan anticipated this case explicitly)
**Impact on plan:** No scope creep. The plan pre-documented this exact case.

## Issues Encountered
None beyond the anticipated clippy behavior.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 01 complete; all 8 requirements satisfied (D-01 through D-06, D-14, D-32)
- Clean Rust source ready for Plan 02 (remaining dead code cleanup candidates)
- `cargo clippy -- -D warnings` clean; `cargo test` 87 tests green

## Self-Check: PASSED

- FOUND: .planning/phases/12.1-clean-up-in-aisle-9/12.1-01-SUMMARY.md
- FOUND: src/server/mod.rs
- FOUND commit: 998869c (refactor: remove dead code)
- FOUND commit: 7253741 (test: TDD RED X-Powered-By)
- FOUND commit: da7886f (feat: X-Powered-By header)

---
*Phase: 12.1-clean-up-in-aisle-9*
*Completed: 2026-03-25*
