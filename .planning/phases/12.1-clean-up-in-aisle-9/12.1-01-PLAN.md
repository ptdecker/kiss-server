---
phase: 12.1-clean-up-in-aisle-9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/url/mod.rs
  - src/time/mod.rs
  - src/server/response.rs
  - src/server/mod.rs
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-14, D-32]

must_haves:
  truths:
    - "cargo clippy -- -D warnings passes with zero warnings after dead code annotations removed"
    - "cargo test passes (86+ tests) confirming no production code was broken"
    - "X-Powered-By header appears in normal HTTP responses"
    - "TODO comment blocks in server/mod.rs are gone"
  artifacts:
    - path: "src/url/mod.rs"
      provides: "pct_encode and pct_decode relocated inside #[cfg(test)] block"
      contains: "#[cfg(test)]"
    - path: "src/time/mod.rs"
      provides: "Dead DateTime accessors removed, allow(dead_code) removed from used fns"
    - path: "src/server/response.rs"
      provides: "allow(dead_code) removed from impl Response block"
    - path: "src/server/mod.rs"
      provides: "ENABLE_POWERED_BY const, X-Powered-By header injection, TODO blocks deleted"
      contains: "ENABLE_POWERED_BY"
  key_links:
    - from: "src/server/mod.rs"
      to: "src/time/mod.rs"
      via: "to_imf_fixdate() call"
      pattern: "dt\\.to_imf_fixdate"
    - from: "src/server/mod.rs"
      to: "src/server/response.rs"
      via: "ctx.response.add_header"
      pattern: "add_header.*X-Powered-By"
---

<objective>
Clean up dead code across Rust source files (D-01 through D-06), delete TODO comment blocks (D-14),
and add the X-Powered-By response header (D-32).

Purpose: Remove all #[allow(dead_code)] suppressions that hide genuinely unused code, relocate
test-only helpers into #[cfg(test)] blocks, and add the single new server behavior for this phase.

Output: Clean Rust source with no dead_code suppressions on unused code, and a working X-Powered-By header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-clean-up-in-aisle-9/12.1-CONTEXT.md
@src/url/mod.rs
@src/time/mod.rs
@src/server/response.rs
@src/server/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead code and relocate test-only functions</name>
  <files>src/url/mod.rs, src/time/mod.rs, src/server/response.rs, src/server/mod.rs</files>
  <read_first>src/url/mod.rs, src/time/mod.rs, src/server/response.rs, src/server/mod.rs</read_first>
  <action>
**src/url/mod.rs (D-01):**
- Cut the `pct_encode` function (lines 80-87) and `pct_decode` function (lines 92-118) — including their doc comments and the `#[allow(dead_code)]` annotations.
- Paste both functions inside the existing `#[cfg(test)]` block (which starts at line 120). Place them before the test functions, after `use super::*;`.
- Remove the `use std::str::from_utf8;` import from the top of the file — it is only used by `pct_decode`. Add `use std::str::from_utf8;` inside the `#[cfg(test)]` block instead (or use `std::str::from_utf8` inline).

**src/time/mod.rs (D-02, D-03):**
- On `to_imf_fixdate()` (line 259): remove both `#[allow(dead_code)]` and `#[allow(clippy::wrong_self_convention)]`. Keep the function itself. Note: the clippy allow was suppressing a false positive about `to_` prefix convention for `&self` methods — removing it should be safe since `to_imf_fixdate` returns an owned `String` which is the correct `to_` convention. If clippy complains, re-add only the clippy allow.
- On `weekday_from_days()` (line 189): remove `#[allow(dead_code)]`. Keep the function.

**src/time/mod.rs (D-04):**
- Delete the entire `days_in_month` function (lines 135-162) including its doc comment and `#[allow(dead_code)]` annotation. No callers exist.

**src/time/mod.rs (D-05):**
- Delete these five DateTime accessor methods and their doc comments and `#[allow(dead_code)]` annotations:
  - `pub fn year(&self) -> u16` (lines 225-229)
  - `pub fn month(&self) -> Month` (lines 231-235)
  - `pub fn day(&self) -> u8` (lines 237-241)
  - `pub fn day_of_year(&self) -> u16` (lines 243-247)
  - `pub fn is_leap_year(&self) -> bool` (lines 249-253)
- No callers exist for any of these.

**src/server/response.rs (D-06):**
- Remove the `#[allow(dead_code)]` annotation on line 16 (above `impl Response`).
- Run `cargo clippy -- -D warnings` to check if any methods in the impl block are surfaced as unused.
- If clippy surfaces unused methods, evaluate each: if genuinely unused in production AND tests, delete it. If used in tests only, add `#[cfg(test)]` or `#[allow(dead_code)]` with a comment explaining why.

**src/server/mod.rs (D-14):**
- Delete the two TODO comment blocks at lines 42-48:
  ```
  // TODO: HTTP/1.1 Support
  //  https://www.rfc-editor.org/rfc/rfc9110.txt (HTTP Semantics)
  //  https://www.rfc-editor.org/rfc/rfc9111.txt (Caching)
  //  https://www.rfc-editor.org/rfc/rfc9112.txt (HTTP/1.1)
  //      Older: https://www.rfc-editor.org/rfc/rfc2068.txt
  // TODO: URI: https://www.rfc-editor.org/rfc/rfc3986.txt
  //  https://www.rfc-editor.org/rfc/rfc6454.txt (origin rules)
  ```
- These are replaced by GitHub issues #24 and #25.
  </action>
  <verify>
    <automated>cd /Users/todddecker/rust/kiss-server && cargo clippy -- -D warnings 2>&1 && cargo test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- src/url/mod.rs does NOT contain `#[allow(dead_code)]` outside of `#[cfg(test)]`
- src/url/mod.rs contains `fn pct_encode` inside `#[cfg(test)]` block
- src/url/mod.rs contains `fn pct_decode` inside `#[cfg(test)]` block
- src/time/mod.rs does NOT contain `fn days_in_month`
- src/time/mod.rs does NOT contain `fn year(&self)`
- src/time/mod.rs does NOT contain `fn month(&self)`
- src/time/mod.rs does NOT contain `fn day(&self)`
- src/time/mod.rs does NOT contain `fn day_of_year(&self)`
- src/time/mod.rs does NOT contain `fn is_leap_year(&self)`
- src/time/mod.rs line for `fn to_imf_fixdate` does NOT have `#[allow(dead_code)]` above it
- src/time/mod.rs line for `fn weekday_from_days` does NOT have `#[allow(dead_code)]` above it
- src/server/response.rs does NOT contain `#[allow(dead_code)]` on the `impl Response` block
- src/server/mod.rs does NOT contain `// TODO: HTTP/1.1`
- src/server/mod.rs does NOT contain `// TODO: URI:`
- `cargo clippy -- -D warnings` exits 0
- `cargo test` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All dead code annotations removed, unused functions deleted, test-only functions relocated, TODO blocks deleted, clippy and all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add X-Powered-By response header</name>
  <files>src/server/mod.rs</files>
  <read_first>src/server/mod.rs</read_first>
  <behavior>
    - Test 1: When ENABLE_POWERED_BY is true, a normal HTTP response contains `X-Powered-By: kiss-serve/` followed by a version string
    - Test 2: The header value matches the pattern `kiss-serve/` + version (e.g., `kiss-serve/0.1.0`)
  </behavior>
  <action>
**Add compile-time constant (D-32):**
- Add `const ENABLE_POWERED_BY: bool = true;` at module scope in `src/server/mod.rs`, immediately after `const DEFAULT_POOL_SIZE: usize = 4;` (which will be around line 40 after Task 1 deleted the TODO blocks).

**Add header injection in handle_connection:**
- In the `handle_connection` function, immediately after the Date header injection block (the `if let Ok(dt) = DateTime::now()` block around line 210-213), add:
  ```rust
  if ENABLE_POWERED_BY {
      ctx.response.add_header("X-Powered-By", concat!("kiss-serve/", env!("CARGO_PKG_VERSION")));
  }
  ```
- Note: uses `kiss-serve` (not `kiss-server`) per user spec. The `env!("CARGO_PKG_VERSION")` macro reads from Cargo.toml at compile time and produces `"0.1.0"`.
- Note: `send_error_response` does NOT get this header per D-32 — error responses are built separately.

**Add integration test:**
- In the existing `#[cfg(test)] mod tests` block in `src/server/mod.rs`, add a test that:
  1. Spawns a test server connection using the existing `spawn_handle_connection_test` helper
  2. Sends a valid GET request
  3. Asserts the response contains `X-Powered-By: kiss-serve/`
- Follow the existing test patterns in the file (use `spawn_handle_connection_test` with a router that has a simple handler or fallback).
  </action>
  <verify>
    <automated>cd /Users/todddecker/rust/kiss-server && cargo test -- --test-threads=1 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
- src/server/mod.rs contains `const ENABLE_POWERED_BY: bool = true;`
- src/server/mod.rs contains `add_header("X-Powered-By", concat!("kiss-serve/", env!("CARGO_PKG_VERSION")))`
- src/server/mod.rs contains a test function that asserts `X-Powered-By` is present in responses
- `cargo test` exits 0 with all tests passing (including the new X-Powered-By test)
- `cargo clippy -- -D warnings` exits 0
  </acceptance_criteria>
  <done>X-Powered-By header injected in normal responses when ENABLE_POWERED_BY is true. Integration test confirms header presence. Error responses do not include the header.</done>
</task>

</tasks>

<verification>
- `cargo clippy -- -D warnings` passes with zero warnings
- `cargo test` passes with all tests (86+ original + new X-Powered-By test)
- No `#[allow(dead_code)]` remains on genuinely dead code
- No TODO comment blocks remain in src/server/mod.rs
- `grep -rn 'allow(dead_code)' src/` shows only intentional annotations (router.rs:51 and handlers/mod.rs:13)
</verification>

<success_criteria>
All Rust dead code suppressions removed or relocated, unused functions deleted, TODO blocks deleted, X-Powered-By header working with test coverage, clippy clean, all tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-clean-up-in-aisle-9/12.1-01-SUMMARY.md`
</output>
