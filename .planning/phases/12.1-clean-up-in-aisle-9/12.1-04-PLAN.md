---
phase: 12.1-clean-up-in-aisle-9
plan: 04
type: execute
wave: 2
depends_on: ["12.1-03"]
files_modified:
  - Justfile
  - CLAUDE.md
  - README.md
  - docs/ci-cd.md
  - docs/design.md
  - scripts/README.md
autonomous: true
requirements: [D-26, D-27, D-28, D-29, D-30, D-31]

must_haves:
  truths:
    - "No prose slash shorthand (and/or, build/run, etc.) remains in user-facing markdown"
    - "Justfile has all 10 recipes: lint, build, run, test, build-docs, docs, verify-dns, branch-protection, prod-protection, deploy"
    - "CLAUDE.md documents all Just commands"
    - "docs/ci-cd.md documents pipeline-related Just commands"
    - "README.md documents non-pipeline Just commands"
  artifacts:
    - path: "Justfile"
      provides: "Expanded recipes including deploy, verify-dns, branch-protection, prod-protection"
      contains: "deploy"
    - path: "CLAUDE.md"
      provides: "Full Just command reference"
      contains: "just deploy"
    - path: "README.md"
      provides: "Non-pipeline Just commands documented, no prose slash shorthand"
    - path: "docs/ci-cd.md"
      provides: "Pipeline Just commands documented, no prose slash shorthand"
  key_links:
    - from: "Justfile"
      to: "scripts/verify-dns.sh"
      via: "verify-dns recipe"
      pattern: "verify-dns"
    - from: "Justfile"
      to: "scripts/setup-branch-protection.sh"
      via: "branch-protection recipe"
      pattern: "branch-protection"
---

<objective>
Replace prose slash shorthand in user-facing markdown (D-26 through D-28), expand the Justfile with
new recipes (D-29, D-30), and document Just commands in the correct files (D-31).

Purpose: Improve prose quality in all user-facing docs, provide convenient Just recipes for common
operations, and ensure documentation tells users how to use them.

Output: Updated Justfile, clean markdown prose, documented Just commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-clean-up-in-aisle-9/12.1-CONTEXT.md
@Justfile
@CLAUDE.md
@README.md
@docs/ci-cd.md
@docs/design.md
@scripts/README.md

<interfaces>
<!-- Plan 03 creates the semver deploy flow. The deploy recipe must match: -->
<!-- Developer flow (D-24): bump CHANGELOG.md -> commit -> tag (git tag vX.Y.Z) -> push tag and prod -->
<!-- Deploy recipe takes a VERSION argument and runs: git tag vVERSION && git push origin vVERSION origin/main:prod -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Justfile with new recipes and audit existing ones</name>
  <files>Justfile</files>
  <read_first>Justfile, .github/workflows/cd.yml</read_first>
  <action>
**Audit existing recipes (D-29):**
Current Justfile has 6 recipes: `lint`, `build`, `run`, `test`, `build-docs`, `docs`.
- `lint`: `cargo fmt` then `cargo clippy` — correct, keep.
- `build`: depends on lint, runs `cargo build` — correct, keep.
- `run`: depends on build, runs `cargo run` — correct, keep. Note: `cargo run` without `--root` will fail at runtime but that is expected (user must pass args).
- `test`: depends on build, runs `cargo test` — correct, keep.
- `build-docs`: depends on lint, runs `cargo doc --lib --document-private-items --no-deps` — correct, keep. Note: `--lib` may not work for a binary crate. If it fails, change to `cargo doc --document-private-items --no-deps`. Test by running `just build-docs`.
- `docs`: depends on build-docs, runs `open target/doc/kiss-server/index.html` — correct for macOS, keep.

**Add new recipes (D-30):**
Add these four recipes after the existing ones:

```just
# Verify DNS records for ptodd.org
verify-dns:
    @./scripts/verify-dns.sh

# Apply branch protection rules to main
branch-protection:
    @./scripts/setup-branch-protection.sh

# Apply branch protection rules to prod
prod-protection:
    @./scripts/setup-prod-protection.sh

# Tag a release and deploy to production
deploy VERSION:
    git tag v{{VERSION}}
    git push origin v{{VERSION}} origin/main:prod
```

The `deploy` recipe (D-30):
- Takes a required `VERSION` argument (e.g., `just deploy 1.2.0`)
- Runs `git tag vVERSION` to create the semver tag
- Runs `git push origin vVERSION origin/main:prod` to push the tag and promote main to prod in one command
- Both commands must succeed; Just stops on first failure by default

Final Justfile should have 10 recipes total.
  </action>
  <verify>
    <automated>cd /Users/todddecker/rust/kiss-server && grep -c '^[a-z]' Justfile | grep -q '10' || (echo "Expected 10 recipes" && grep '^[a-z]' Justfile && false) && grep -q 'deploy VERSION' Justfile && grep -q 'verify-dns' Justfile && grep -q 'branch-protection' Justfile && grep -q 'prod-protection' Justfile && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- Justfile contains `deploy VERSION:` recipe
- Justfile deploy recipe contains `git tag v{{VERSION}}`
- Justfile deploy recipe contains `git push origin v{{VERSION}} origin/main:prod`
- Justfile contains `verify-dns:` recipe that runs `./scripts/verify-dns.sh`
- Justfile contains `branch-protection:` recipe that runs `./scripts/setup-branch-protection.sh`
- Justfile contains `prod-protection:` recipe that runs `./scripts/setup-prod-protection.sh`
- Justfile retains all 6 original recipes: lint, build, run, test, build-docs, docs
  </acceptance_criteria>
  <done>Justfile has 10 recipes: 6 original (audited) plus 4 new (verify-dns, branch-protection, prod-protection, deploy).</done>
</task>

<task type="auto">
  <name>Task 2: Replace prose slash shorthand in all user-facing markdown</name>
  <files>README.md, CLAUDE.md, docs/design.md, docs/ci-cd.md, scripts/README.md</files>
  <read_first>README.md, CLAUDE.md, docs/design.md, docs/ci-cd.md, scripts/README.md</read_first>
  <action>
**Scope (D-26, D-27):** User-facing `.md` files only: README.md, CLAUDE.md, docs/design.md, docs/ci-cd.md, scripts/README.md. Exclude .planning/ (internal tooling).

**Exception (D-28):** Do NOT change:
- Technical path notation: `src/server/mod.rs`, `/var/www/`, `/usr/local/bin/`
- URL paths: `http://ptodd.org/`
- Established acronyms where slash is part of the name: `CI/CD` as a proper noun in headings or badge references, `HTTP/1.1`, `I/O`
- Code blocks and inline code

**What to replace:** Only prose shorthand where `/` means "or", "and", or "and or". Examples:
- "build/run" -> "build and run" or "build or run" (context-dependent)
- "and/or" -> "or" or "and" (context-dependent; usually "or" suffices)
- "push/pull" -> "push and pull" or "push or pull"
- "pass/fail" -> "pass or fail"
- "stop/start" -> "stop and start"
- "request/response" -> "request and response" (when used as prose, not as a technical compound)

Specific instances to check and fix in each file:

**README.md:**
- Line 5: "...RFC-compliant HTTP/1.1 response" — keep `HTTP/1.1` (technical)
- Line 31: "builds a release binary, deploys it to EC2, verifies the service is running, and creates a GitHub Release" — no slash, OK
- Line 35: "Handler/Context/Router abstraction" — this is a compound proper noun for the pattern name. Replace with "Handler, Context, and Router abstraction".
- Line 1: `CI` badge — keep as-is (URL)

**CLAUDE.md:**
- Line 24: "CI and CD pipelines" — already correct, no slash
- Line 30: "Repo Layout" section — check each bullet for prose slashes

**docs/design.md:**
- Line 9: "Handler / Context / Router" heading area — replace prose instances with "Handler, Context, and Router"
- Line 12: "request/response cycle" — replace with "request and response cycle"
- Line 82: "`write_to` consumes `Response` (`self`)" — no slash, OK
- Line 88: "404 on path traversal (not 403)" — no slash, OK
- Scan for any other prose slash instances

**docs/ci-cd.md:**
- Line 1: "# CI/CD Pipeline" — keep as heading (proper noun, established acronym)
- Line 7: "push to `main` and every pull request" — already correct
- Line 37: "GitHub Release tagged `deploy/{sha}`" — will be updated by Plan 03 anyway; this is a tag format, not prose
- Line 39: "Check the GitHub Actions CD run" — no slash
- Scan for any other prose slash instances

**scripts/README.md:**
- Line 1: "# Scripts" — no slash
- Scan table cells for any prose slash shorthand
- Line 9: "CI/CD setup" in the heading area — keep as proper noun

Use best English judgment per context for each replacement. When in doubt, "and" for things that happen together, "or" for alternatives.
  </action>
  <verify>
    <automated>cd /Users/todddecker/rust/kiss-server && for f in README.md CLAUDE.md docs/design.md docs/ci-cd.md scripts/README.md; do echo "=== $f ===" && grep -n '[a-z]/[a-z]' "$f" | grep -v '^\s*#' | grep -v 'http' | grep -v 'src/' | grep -v '/var/' | grep -v '/usr/' | grep -v '/tmp/' | grep -v '/opt/' | grep -v '`.*/.*`' | grep -v 'CI/CD' | grep -v 'HTTP/1' | grep -v 'I/O' | grep -v '/dev/' | grep -v '/etc/' || echo "(clean)"; done</automated>
  </verify>
  <acceptance_criteria>
- README.md does not contain "Handler/Context/Router" (replaced with "Handler, Context, and Router")
- docs/design.md does not contain prose "request/response" outside code blocks (replaced with "request and response")
- No file contains "and/or" in prose
- No file contains "build/run" or "stop/start" or "pass/fail" in prose
- Technical paths (src/server/mod.rs, /var/www/, HTTP/1.1, CI/CD as proper noun) remain unchanged
  </acceptance_criteria>
  <done>All prose slash shorthand replaced with proper English in user-facing markdown. Technical paths and established acronyms preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Document Just commands in CLAUDE.md, docs/ci-cd.md, and README.md</name>
  <files>CLAUDE.md, docs/ci-cd.md, README.md</files>
  <read_first>CLAUDE.md, docs/ci-cd.md, README.md, Justfile</read_first>
  <action>
**D-31: Documentation placement for Just commands.**

**CLAUDE.md — all Just commands (for Claude's reference):**
Add a new `## Just Commands` section after the existing `## Testing` section. List all 10 recipes:

```markdown
## Just Commands

Development:
- `just lint` — format and lint (`cargo fmt` + `cargo clippy`)
- `just build` — lint then build
- `just run` — lint, build, then run
- `just test` — lint, build, then test
- `just build-docs` — lint then generate rustdoc
- `just docs` — generate and open rustdoc in browser

Infrastructure:
- `just verify-dns` — run DNS smoke tests (`scripts/verify-dns.sh`)
- `just branch-protection` — apply main branch protection rules (`scripts/setup-branch-protection.sh`)
- `just prod-protection` — apply prod branch protection rules (`scripts/setup-prod-protection.sh`)

Deployment:
- `just deploy <VERSION>` — tag `vVERSION` and push to prod (e.g., `just deploy 1.2.0`)
```

**docs/ci-cd.md — pipeline-related Just commands (D-31):**
Add a new section `## Just Recipes` at the end of the file (before any trailing whitespace). Include only the pipeline-related commands:

```markdown
## Just Recipes

Convenience recipes for pipeline and infrastructure operations:

- `just deploy <VERSION>` — tag the current commit as `vVERSION` and push to prod, triggering the CD pipeline (e.g., `just deploy 1.2.0`)
- `just verify-dns` — run DNS smoke tests for ptodd.org and www.ptodd.org
- `just branch-protection` — apply or update main branch protection rules via GitHub API
- `just prod-protection` — apply or update prod branch protection rules via GitHub API
```

**README.md — non-pipeline commands (D-31):**
Add a `## Development` section after the existing `## Build & Run Locally` section (or rename that section). Include the non-pipeline Just commands:

```markdown
## Development

[Just](https://github.com/casey/just) recipes for common tasks:

```bash
just lint          # Format and lint (cargo fmt + cargo clippy)
just build         # Lint then build
just run           # Lint, build, then run
just test          # Lint, build, then test
just build-docs    # Generate rustdoc
just docs          # Generate and open rustdoc in browser
```
```

Also update the Deployment section in README.md to reference the new deploy recipe:

Change the deploy command from:
```bash
git push origin origin/main:prod
```
To:
```bash
just deploy 1.2.0  # tags v1.2.0 and pushes to prod
```

And update the surrounding prose to mention that CHANGELOG.md must be updated before deploying (per D-22/D-24).

Also update the `.planning/` line in the Repo Layout section of CLAUDE.md. Since .planning/ is now untracked (Plan 02, D-15/D-16), update the description:
- Change: `- `.planning/` — GSD project management artifacts`
- To: `- `.planning/` — GSD project management artifacts (local only, gitignored)`
  </action>
  <verify>
    <automated>cd /Users/todddecker/rust/kiss-server && grep -q 'just deploy' CLAUDE.md && grep -q 'just deploy' docs/ci-cd.md && grep -q 'just deploy' README.md && grep -q 'just lint' README.md && grep -q 'just verify-dns' CLAUDE.md && grep -q 'just verify-dns' docs/ci-cd.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- CLAUDE.md contains `## Just Commands` section listing all 10 recipes
- CLAUDE.md contains `just deploy`, `just lint`, `just verify-dns`, `just branch-protection`, `just prod-protection`
- docs/ci-cd.md contains `## Just Recipes` section listing deploy, verify-dns, branch-protection, prod-protection
- README.md contains `just lint`, `just build`, `just run`, `just test`, `just build-docs`, `just docs`
- README.md deployment section references `just deploy` instead of bare `git push origin origin/main:prod`
- README.md deployment section mentions CHANGELOG.md must be updated before deploying
- CLAUDE.md .planning/ description updated to note "local only, gitignored"
  </acceptance_criteria>
  <done>Just commands documented in correct locations: all in CLAUDE.md, pipeline in docs/ci-cd.md, development in README.md. Deploy instructions updated to use just deploy recipe.</done>
</task>

</tasks>

<verification>
- `just --list` shows all 10 recipes
- No prose slash shorthand in any user-facing markdown (verified by grep)
- CLAUDE.md, docs/ci-cd.md, and README.md each document the correct subset of Just commands
- README.md deploy instructions reference the new semver workflow
</verification>

<success_criteria>
Justfile has 10 working recipes. All prose slash shorthand replaced in user-facing markdown. Just commands documented in CLAUDE.md (all), docs/ci-cd.md (pipeline), and README.md (development). Deploy flow documentation updated to reflect semver tagging requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-clean-up-in-aisle-9/12.1-04-SUMMARY.md`
</output>
