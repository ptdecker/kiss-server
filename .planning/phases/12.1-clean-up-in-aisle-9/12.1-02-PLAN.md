---
phase: 12.1-clean-up-in-aisle-9
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - test.js
  - hello.html
  - 404.html
  - aws-ec2.sh
  - HOSTING.md
  - package-lock.json
autonomous: true
requirements: [D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-15, D-16]

must_haves:
  truths:
    - ".DS_Store is gitignored and will not appear in future git status"
    - "Stale root files (test.js, hello.html, 404.html, aws-ec2.sh, HOSTING.md, package-lock.json) no longer exist in the working tree"
    - ".planning/ directory exists locally but is invisible to git"
  artifacts:
    - path: ".gitignore"
      provides: "Updated gitignore with .DS_Store and .planning/ entries"
      contains: ".DS_Store"
    - path: ".gitignore"
      provides: ".planning/ ignored"
      contains: ".planning/"
  key_links: []
---

<objective>
Clean up the repository root by deleting stale files (D-08 through D-13), updating .gitignore (D-07,
D-15), and untracking .planning/ from git (D-16).

Purpose: Remove files that serve no purpose in the repo (test.js, hello.html, 404.html, aws-ec2.sh,
HOSTING.md, package-lock.json), add .DS_Store to gitignore, and make .planning/ a local-only directory.

Output: Clean repo root with correct .gitignore entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-clean-up-in-aisle-9/12.1-CONTEXT.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore and delete stale root files</name>
  <files>.gitignore, test.js, hello.html, 404.html, aws-ec2.sh, HOSTING.md, package-lock.json</files>
  <read_first>.gitignore</read_first>
  <action>
**Update .gitignore (D-07, D-15):**
- Add `.DS_Store` on its own line (D-07).
- Add `.planning/` on its own line (D-15).
- Current .gitignore contents:
  ```
  /bhbackup
  /.vscode
  .idea
  /target
  bluehost_backup
  ```
- Add the two new entries at the end. Final file:
  ```
  /bhbackup
  /.vscode
  .idea
  /target
  bluehost_backup
  .DS_Store
  .planning/
  ```

**Delete stale files (D-08 through D-13):**
- `git rm test.js` — not referenced anywhere; JS MIME tests use in-memory fixtures (D-08)
- `git rm hello.html` — not the production Hello World page; that is generated by scripts/setup-webroot.sh heredoc (D-09)
- `git rm 404.html` — 404 responses are generated programmatically (D-10)
- `git rm aws-ec2.sh` — stray script, not referenced anywhere (D-11)
- `git rm HOSTING.md` — superseded by docs/ci-cd.md (D-12)
- `git rm package-lock.json` — no package.json in the repo (D-13)

Note: Some of these files may be untracked (shown as `??` in git status). For untracked files, just `rm` them (no `git rm` needed). For tracked files, use `git rm`. Check `git ls-files` for each to determine which are tracked.
  </action>
  <verify>
    <automated>cd /Users/todddecker/rust/kiss-server && test ! -f test.js && test ! -f hello.html && test ! -f 404.html && test ! -f aws-ec2.sh && test ! -f HOSTING.md && test ! -f package-lock.json && grep -q '.DS_Store' .gitignore && grep -q '.planning/' .gitignore && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- test.js does not exist in repo root
- hello.html does not exist in repo root
- 404.html does not exist in repo root
- aws-ec2.sh does not exist in repo root
- HOSTING.md does not exist in repo root
- package-lock.json does not exist in repo root
- .gitignore contains `.DS_Store`
- .gitignore contains `.planning/`
  </acceptance_criteria>
  <done>All six stale files deleted, .gitignore updated with .DS_Store and .planning/ entries.</done>
</task>

<task type="auto">
  <name>Task 2: Untrack .planning/ directory from git</name>
  <files>.planning/</files>
  <read_first>.gitignore</read_first>
  <action>
**Untrack .planning/ (D-16):**
- Run `git rm -r --cached .planning/` to remove all .planning/ files from the git index without deleting them from disk.
- This makes .planning/ invisible to git going forward (since .planning/ is already in .gitignore from Task 1).
- Verify with `git status` that .planning/ files show as "deleted" in the staging area (these are index-only deletions — files remain on disk).
- Verify with `ls .planning/` that all files still exist locally.

IMPORTANT: Do NOT run `git rm -r .planning/` (without `--cached`) — that would delete the files from disk.
  </action>
  <verify>
    <automated>cd /Users/todddecker/rust/kiss-server && git ls-files .planning/ | head -5 | wc -l | tr -d ' ' | grep -q '^0$' && test -d .planning/ && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- `git ls-files .planning/` returns empty output (no tracked files under .planning/)
- `ls .planning/STATE.md` confirms file still exists on disk
- `ls .planning/ROADMAP.md` confirms file still exists on disk
- `.planning/` directory is intact locally
  </acceptance_criteria>
  <done>.planning/ directory is untracked from git but remains intact locally. Future git operations ignore it.</done>
</task>

</tasks>

<verification>
- `git status` shows no .planning/ files as tracked
- `ls .planning/` shows all files intact
- Root directory has no stale files (test.js, hello.html, 404.html, aws-ec2.sh, HOSTING.md, package-lock.json)
- .gitignore contains .DS_Store and .planning/ entries
</verification>

<success_criteria>
Repo root is clean of stale files, .gitignore properly excludes .DS_Store and .planning/, the .planning/ directory is untracked but intact locally.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-clean-up-in-aisle-9/12.1-02-SUMMARY.md`
</output>
