# Phase 12.1: clean up in aisle 9 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-25
**Phase:** 12.1-clean-up-in-aisle-9
**Areas discussed:** pct_encode/pct_decode, time module dead code, stray root files,
TODO comments, CI/CD deprecation warnings, GitHub release naming, markdown "/" style, Justfile

---

## pct_encode / pct_decode

| Option | Description | Selected |
|--------|-------------|----------|
| Move into `#[cfg(test)]` | Correct placement — only called from tests | ✓ |
| Delete entirely | Would break `encode` and `decode` tests | |
| Keep with `#[allow(dead_code)]` | Status quo — misleads readers | |

**User's choice:** Move into `#[cfg(test)]`
**Notes:** User initially asked "if they aren't used, how are we safely encoding and
decoding URLs?" — investigation confirmed they ARE used in tests (lines 201–212 of
url/mod.rs). Production decoding is via `Url::decoded_path()` / `hex_char_to_byte()`.

---

## time module dead code

| Function | Used where | Decision | Selected |
|----------|-----------|----------|----------|
| `to_imf_fixdate()` | Production (server/mod.rs:111,212) + tests | Keep, remove `#[allow]` | ✓ |
| `weekday_from_days()` | Called by `to_imf_fixdate()` | Keep, remove `#[allow]` | ✓ |
| `days_in_month()` | Nowhere | Delete | ✓ |
| `year()`, `month()`, `day()`, `day_of_year()`, `is_leap_year()` | Nowhere | Delete | ✓ |

**User's choice:** Things used in tests → `#[cfg(test)]`; things not used at all → remove.
**Notes:** User asked "should we keep these for later?" — confirmed they are trivial to
recreate (one-liner accessors wrapping existing struct fields). Binary crate, not a library.
Decision: delete rather than keep as noise.

---

## Stray Root Files

| File | Decision | Selected |
|------|----------|----------|
| `.DS_Store` | Add to `.gitignore` | ✓ |
| `test.js` | Delete — not referenced anywhere | ✓ |
| `hello.html` | Delete — production Hello World comes from `setup-webroot.sh` heredoc | ✓ |
| `404.html` | Delete — 404s generated programmatically | ✓ |
| `aws-ec2.sh` | Delete — not referenced anywhere | ✓ |
| `HOSTING.md` | Delete — superseded by `docs/ci-cd.md` | ✓ |
| `package-lock.json` | Delete — no `package.json` exists | ✓ |

**Notes:** User asked where the production Hello World comes from — confirmed it is the
heredoc in `scripts/setup-webroot.sh`, not `hello.html`. User also thought `test.js` might
be used for JS file serving tests — confirmed it is not; those tests use in-memory fixtures.

---

## TODO Comments (server/mod.rs)

| Option | Description | Selected |
|--------|-------------|----------|
| Delete + create GitHub issues | Remove noise; preserve actionable gaps as issues | ✓ |
| Keep | Aspirational reference bookmarks | |

**User's choice:** Check GitHub issues first; if not covered, evaluate and create issues.
**Notes:** Issue #1 ("Review in-code TODO items for resolution") covers the review task
but not specific implementations. Two new issues created:
- #24: HTTP/1.1 persistent connections (keep-alive)
- #25: Conditional GET (ETag, Last-Modified, If-Modified-Since)
RFC 3986 and RFC 6454 deemed out of scope for a static file server.

---

## CI/CD Deprecation Warnings

| Warning | Cause | Action | Selected |
|---------|-------|--------|----------|
| Node.js 20 deprecation | `actions/checkout@v4` | Bump to Node.js 24 compatible version | ✓ |
| Node.js 20 deprecation | `softprops/action-gh-release@v2` | Bump to Node.js 24 compatible version | ✓ |
| DEP0040 punycode | Same actions internally | Clears when action versions are bumped | ✓ |

**Notes:** GitHub is forcing Node.js 24 by June 2, 2026. Both CI and CD workflows affected.

---

## GitHub Release Naming

| Option | Description | Selected |
|--------|-------------|----------|
| Date + short SHA | `deploy/20260325-db5ff29` | |
| Semver + short SHA | `vX.Y.Z (db5ff29)` | ✓ |
| Short SHA only | `deploy/db5ff29` | |
| Keep current | Full SHA, unreadable | |

**User's choice:** Semantic versioning from git tags + short SHA: `vX.Y.Z (sha)`.
**Notes:**
- No CHANGELOG.md existed — will be created with Keep a Changelog format.
- CD pipeline must fail if no semver tag on commit.
- CD pipeline must fail if CHANGELOG.md has no entry for the tag.
- CHANGELOG entry for the tag populates the GitHub Release body.
- Developer flow: bump CHANGELOG → commit → tag → push tag + prod.
- Test release `deploy/db5ff29fabe6dc0bcc2be92ebbe2b35e17acaefd` deleted during session.
- Initial CHANGELOG.md will include entries for v1.0.0 and v1.1.0.

---

## Markdown "/" Style

| Option | Description | Selected |
|--------|-------------|----------|
| Replace in user-facing .md files only | README.md, CLAUDE.md, docs/, scripts/README.md | ✓ |
| Replace everywhere including .planning/ | All markdown in repo | |

**User's choice:** User-facing markdown only; `.planning/` excluded (internal tooling).
**Notes:** Exception for technical path notation, URL paths, and established acronyms
where the slash is structural (e.g., `/var/www/`, `src/server/mod.rs`).

---

## Justfile

| Decision | Option chosen |
|---------|--------------|
| Keep or drop | Keep |
| New recipes | `verify-dns`, `branch-protection`, `prod-protection`, `deploy` |
| Documentation — pipeline commands | `docs/ci-cd.md` |
| Documentation — dev commands | `README.md` |
| Documentation — all commands | `CLAUDE.md` |

**Notes:** `deploy` recipe should accept a version argument and handle both tagging
and the prod push atomically. EC2-side scripts (install, webroot, iptables, infra setup)
are not candidates for Just recipes — they run on the EC2 instance, not locally.

---

## Claude's Discretion

- Exact CHANGELOG.md content for v1.0.0 and v1.1.0 entries
- Shell logic for tag validation and CHANGELOG entry extraction in CD pipeline
- Exact wording of prose "/" replacements
- Which existing Just commands need corrections after audit

## Deferred Ideas

- Range requests, chunked transfer encoding — tracked in GitHub backlog
- "Python Version" historic release cleanup — low priority
